# ACTIVITY — Shams.ma



## 2026-07-21 — Session 1 (Sprint 1 kickoff)
- DECISION: Stack pivot confirmed — Java Spring Boot backend, React frontend, Docker deployment, PostgreSQL+PostGIS retained. Supersedes README's Next.js/Drizzle stack.
- ENV_VARS_COLLECTED (draft): DB_*, JWT_*, CMI_* (pending scope decision), GEOCODING_API_KEY, SMTP_*, FILE_STORAGE_*, SERVER_PORT, CORS_ALLOWED_ORIGINS — written to .env.example. Real values deferred to before EXECUTE (Session 2).
- Drafted docs/prd-shams-ma.md (PM) — awaiting user approval. 2 open questions raised (payments in scope? coverage-zone model?).

## 2026-07-21 (cont.)
- MILESTONE: PRD approved (with 2 amendments: payments IN scope via CMI deposit; coverage zones = radius-from-point).
- MILESTONE: System design approved (single monolith, single region, no queue/cache — YAGNI).
- MILESTONE: Architecture approved (Layered/package-by-feature, JWT auth, React feature-folders + React Query).
- MILESTONE: Security baseline approved (MFA for Admin only, JWT, webhook signature verification, signed URLs for cert docs).
- MILESTONE: Database design approved (PostgreSQL+PostGIS, 9 tables, Flyway migrations, GiST index for coverage matching).
- MILESTONE: UX foundation approved (3 personas, 3 core flows, 4 key screen wireframes + states).
- MILESTONE: UI foundation approved (MUI framework, solar-gold/renewable-green theme).
- MILESTONE: Test strategy approved (Payment=Maximum rigor, 80% coverage gate, ATDD scenarios for booking/payment/cert/coverage-match).
- MILESTONE: DevOps foundation approved (GitHub Actions CI, Docker for backend+frontend, Postgres+PostGIS compose, monitoring baseline).
- MILESTONE: Stories ready (approved). All 10 foundation docs now complete: prd, system-design, architecture, security, database, ux, ui, test-strategy, devops, stories.
- PUSH: main -> origin/main. Commits: bf17186 (chore: initial commit — framework/README/env), 384dd8d (docs: foundation documents for shams-ma). Repo: https://github.com/rhorba/shams-ma

## 2026-07-22 — Session 2 (Sprint 2 kickoff)
- ENV_VARS: Real local-dev values collected/generated. DB_USER/PASSWORD=postgres/postgres. JWT_SECRET auto-generated (256-bit, written to .env only). CMI=sandbox/test placeholder creds. Geocoding=Nominatim (free, no key). SMTP=Mailhog (local catcher). File storage=MinIO (local S3-compatible). All written to .env (gitignored, not committed) and .env.example (placeholders/defaults only).
- NOTE: skills/ directory found deleted on disk (duplicated into untracked .claude/.skills/, not picked up by Skill tool). User chose to leave as-is and proceed — revisit later if needed.

## 2026-07-22 — Story 0.1: Project scaffold + CI pipeline
- Backend: Spring Boot 4.1.0 (Java 21 target, Maven wrapper) scaffolded via start.spring.io with web/security/data-jpa/validation/actuator/flyway/postgresql/testcontainers. Added nimbus-jose-jwt (RS256, for Story 0.3), Spotless (googleJavaFormat, v3.8.0 — v2.43.0 incompatible with JDK25 javac internals, bumped), JaCoCo 0.8.12 with 80% instruction-coverage gate (ApiApplication.class excluded from the ratio — boilerplate bootstrap, not business logic). Package-by-feature skeleton created: auth, homeowner, installer, booking, payment, admin, notification, shared (ADR-1/ADR-3). application.yml wired to .env var names.
- Frontend: Vite + React 19 + TypeScript scaffolded (`npm create vite`). Kept the template's built-in oxlint (deviates from plan's "ESLint+Prettier" — oxlint satisfies the same `npm run lint` CI contract, faster, avoids duplicate tooling). Added @tanstack/react-query, Vitest + React Testing Library + jsdom + v8 coverage, feature-folder skeleton (src/app/, src/features/), placeholder App + passing test.
- Docker: backend/Dockerfile, frontend/Dockerfile copied verbatim from devops doc. docker-compose.yml at repo root: db/api/web per doc plus mailhog + minio added for local dev parity with .env.
- CI: .github/workflows/ci.yml copied from devops doc (backend/frontend/security/build jobs); added `defaults.run.working-directory` per backend/frontend job (required for `./mvnw`/`npm` to resolve — not explicit in the doc's snippet but necessary).
- README stack section corrected (was still Next.js/Drizzle) to Java Spring Boot + React/Vite + PostgreSQL/PostGIS + Docker + CMI.
- Deleted stray bash.exe.stackdump.
- VERIFY: `./mvnw -DskipTests package` pass. `./mvnw spotless:check` pass. `./mvnw test jacoco:report` + `./mvnw jacoco:check` pass EXCLUDING the Testcontainers-backed ApiApplicationTests (this sandbox shell has no reachable Docker daemon — confirmed via `docker ps` failure — so that one test can't run here; it needs Docker, which CI/GitHub Actions runners and any real dev machine provide). `npm run build`/`npm test -- --coverage`/`npm run lint` all pass. `docker compose config` validates clean. CI workflow YAML parses valid.

## 2026-07-22 (cont.) — Story 0.1 checkpoint
- Full `docker-compose up` verification performed (fork's report only validated `docker compose config`, not an actual boot). Found and fixed a real bug: docker-compose.yml's `api` service inherited DB_HOST=localhost/SMTP_HOST=localhost/FILE_STORAGE_ENDPOINT=localhost:9000 from `.env` via env_file, which don't resolve inside the compose network. Fixed with an `environment:` override block on `api` (DB_HOST=db, DB_PORT=5432, SMTP_HOST=mailhog, FILE_STORAGE_ENDPOINT=http://minio:9000).
- Also made api/web/db host port mappings overridable via ${API_HOST_PORT:-8080}/${WEB_HOST_PORT:-4173}/${DB_HOST_PORT:-5432} env vars (defaults match the DevOps doc) — this dev machine already has other local projects bound to 5432/8080, verified using overrides (5435/8090/4173) without changing the committed defaults.
- End-to-end boot confirmed: db healthy, Flyway ran (0 migrations, expected pre-0.2), Hibernate/JPA initialized, Spring Security loaded, GET /actuator/health -> {"status":"UP"}, web container serves 200 on /.

## 2026-07-22 (cont.) — Story 0.2: Database schema + Flyway migrations
- Created V1-V9 in backend/src/main/resources/db/migration/, copied field-for-field from docs/database-shams-ma.md sections 3/4/5 (no deviations from the doc's SQL).
- VERIFY: started db via `DB_HOST_PORT=5435 docker compose up -d db` (healthy), then `docker compose up -d --build api` (Spring Boot's own Flyway-on-boot, since no flyway-maven-plugin is configured — app-boot approach was the working option). Logs confirmed "Successfully applied 9 migrations to schema public, now at version v9". `\dt` confirmed all 9 project tables + flyway_schema_history; `\dT` confirmed all 5 project enums (user_role, verification_status, quote_status, booking_status, payment_status); `\di` confirmed all 8 explicit indexes from section 4 plus the 2 implicit UNIQUE-backed ones (users_email_key, payments_cmi_transaction_id_key). Torn down cleanly with `docker compose down` afterward — only this project's containers touched.

## 2026-07-22 (cont.) — Story 0.3: Auth (registration, login, RS256 JWT, RBAC, admin MFA)
- Built: JPA entities (User/Homeowner/Installer, package-by-feature per ADR-1/ADR-3, HomeownerService/InstallerService interfaces called from auth), POST /api/v1/auth/register (role-specific, bcrypt, breached-password+min-10-char check, Bean Validation), POST /api/v1/auth/login (RS256 access token 15min + refresh token 7d HttpOnly+Secure+SameSite=Strict cookie), Spring Security stateless filter chain (JwtAuthenticationFilter, RateLimitFilter via Bucket4j in-memory on login/register), TOTP admin MFA (dev.samstevens.totp) with POST /api/v1/auth/mfa/enroll + /verify, RSA 2048-bit keypair generated under backend/src/main/resources/keys/ (gitignored) replacing the earlier JWT_SECRET in .env/.env.example with JWT_PRIVATE_KEY_PATH/JWT_PUBLIC_KEY_PATH.
- Unit tests (PasswordPolicy, JwtService, TotpService) + Testcontainers integration tests (AuthControllerIntegrationTest, 6 scenarios: register/login happy path, weak password, duplicate email, wrong password, non-admin 403, admin-without-MFA 403 + enroll flow). All 17 tests pass, JaCoCo 80% gate met, Spotless clean.
- Three real bugs found and fixed during verification (all pre-existing gaps not caught by earlier stories, since this was the first time real request-response flows ran against the stack):
  1. TestcontainersConfiguration (from Story 0.1's Initializr scaffold) used plain `postgres:latest`, incompatible with V1's `CREATE EXTENSION postgis` — switched to `postgis/postgis:16-3.4-alpine` with `asCompatibleSubstituteFor("postgres")`. This also let the original ApiApplicationTests context-load test run for the first time (Story 0.1's fork had no Docker in its sandbox to verify it).
  2. Hibernate needs `@JdbcTypeCode(SqlTypes.NAMED_ENUM)` on fields backed by native Postgres enum columns (user_role, verification_status) — without it, inserts fail with "column is of type X but expression is of type character varying".
  3. Spring Security 403-instead-of-400 bug: `ResponseStatusException(BAD_REQUEST)` triggers Boot's internal forward to `/error`, which re-enters the security filter chain; since `/error` wasn't permitAll'd, unauthenticated access got 403'd by Http403ForbiddenEntryPoint, silently overwriting the intended 400. Fixed by adding `/error` to permitAll. MockMvc-based integration tests did NOT catch this (no real container-level error forwarding in MockMvc) — only caught via manual curl smoke test against the real running docker-compose stack, confirming the plan's "integration tests + manual smoke test" verification was not redundant.
- Manual curl smoke test against the real stack (docker-compose, ports 5435/8090 to avoid this machine's other-project port clashes): register/login/weak-password-400/duplicate-email-409/wrong-password-401/non-admin-403/admin-no-mfa-403/mfa-enroll-200/relogin-mfaEnrolled-true all confirmed. Torn down cleanly after.

## 2026-07-22 (cont.) — CI debugging to green
Pushed Epic 0 (commit 11f564e), CI came up red. Diagnosed and fixed 6 real issues across 6 follow-up commits, monitoring CI after each push per rule 11:
1. a03b21f — Trivy `severity: CRITICAL,HIGH` unquoted in YAML flow-mapping parsed as two keys; backend/mvnw lost its executable bit committing from Windows.
2. c7e4f6c — CI's fresh checkout has no RSA keypair (dev keys correctly gitignored) -> JwtService init failed -> all Spring context tests failed. Added a dedicated test-only keypair at backend/src/test/resources/keys/. Also pre-populated the Maven cache in the security job so Trivy's SCA scan doesn't hit Maven Central live (was getting 429 rate-limited resolving the Spring Boot BOM).
3. 6fb1ff5 — Real CVE: org.postgresql:postgresql 42.7.11 (BOM-managed) has a HIGH SCRAM-SHA-256-PLUS downgrade MITM vuln (CVE-2026-54291), fixed in 42.7.12. Overrode via postgresql.version property.
4. 66d8337 — gitleaks binary wasn't installed on the runner (workflow assumed it pre-existed). Added an install step + .gitleaksignore for 2 verified-safe findings (the test keypair fixture, an empty JWT_SECRET= placeholder in original Sprint-1 .env.example history).
5. f1f2a24 — Shallow checkout (fetch-depth 1 default) meant gitleaks fingerprints weren't stable across pushes (tied to whichever commit happened to be latest). Set fetch-depth: 0 for the security job.
6. becf8f9 — Same "binary not installed" bug as gitleaks, but for the build job's raw `trivy image` CLI call. Switched to aquasecurity/trivy-action (already proven working in the security job).
7. dbc1e9e — 5 HIGH CVEs in golang.org/x/net inside `usr/bin/pebble`, a service-manager binary bundled by the eclipse-temurin:21-jre base image (approved base image per devops doc) — not app-level, not invoked by our entrypoint. Accepted via .trivyignore with justification.

All 6 CI/security-config fixes and 1 real dependency CVE fix are also mirrored in docs/devops-shams-ma.md so future copy-paste doesn't reintroduce the same bugs. Final run (29924938375): all 5 jobs green (frontend, backend, security, build, deploy-staging placeholder).

## 2026-07-23 — Session 3 kickoff
- Verified resumption context: repo clean at 3bc1a6c, no drift from last session-end log. Next unit: Epic 1 (Installer Onboarding & Verification), Stories 1.1-1.3 per docs/stories-shams-ma.md.

## 2026-07-23 (cont.) — Epic 1 backend: Stories 1.1, 1.2, 1.3
- Story 1.1 (coverage zone + browse): shared/geocoding package (GeocodingService + NominatimGeocodingService, Jackson-3-native `tools.jackson.databind.JsonNode` — Spring Boot 4.1 ships Jackson 3, which rebranded groupId/package from com.fasterxml). InstallerRepository gained two native @Query methods (updateCoverageZone via ST_MakePoint, findWithinRadius via ST_DWithin + an interface projection) — same "skip JPA/JTS mapping" approach as migration V10. PUT /api/v1/installer/coverage-zone (ROLE_INSTALLER, radius validated 1-200km) and public GET /api/v1/installers/browse (added to RateLimitFilter's limited paths per Security doc's public-endpoint rate-limiting requirement).
- Story 1.2 (cert upload): added software.amazon.awssdk:s3 2.29.52. shared/storage package: FileStorageService/S3FileStorageService (MinIO via path-style + endpoint override, placeholder Region.US_EAST_1), UploadValidator (magic-byte sniffing for PDF/JPEG/PNG, not the spoofable client Content-Type, + 10MB cap). CertificationDocument entity/repository over the existing certification_documents table. POST /api/v1/installer/certifications (multipart).
- Story 1.3 (admin review): shared/AuditLog(+Repository+Service) over the existing audit_log table. CertificationService (installer package, public interface) does upload/findByStatus/approve/reject — approve/reject are @Transactional and flip both the certification row and the owning installer's verification_status plus write one audit_log row, all in one method. GET/POST /api/v1/admin/certifications(/{id}/approve|reject) (ROLE_ADMIN_MFA).
- Real bugs found and fixed:
  1. Spring Boot 4.1's `spring-boot-starter-webmvc` doesn't autoconfigure a `RestClient.Builder` bean (unlike the old `spring-boot-starter-web`) — NominatimGeocodingService's constructor injection of it failed app context startup entirely (broke ALL SpringBootTests, not just the new ones). Fixed by having the class build its own `RestClient.builder()` internally (production constructor), keeping a second package-private constructor for tests to bind a MockRestServiceServer.
  2. S3FileStorageService's @PostConstruct bucket-auto-create originally had no failure handling — since it's a global singleton bean, it would have broken every SpringBootTest context (including pre-existing Story-0.3 tests) whenever MinIO isn't running for that particular test. Made it best-effort (catch SdkException, log a warning, don't fail startup) plus a 5s API-call timeout so it can't hang context startup.
  3. **Presigned certification-view-URL used the internal Docker service hostname** (`http://minio:9000`, from FILE_STORAGE_ENDPOINT's compose override) — unusable from any browser outside the compose network. Same class of bug as Story 0.1's DB_HOST=localhost issue. Fixed by adding a separate `app.file-storage.public-endpoint` (`FILE_STORAGE_PUBLIC_ENDPOINT` env var, defaults to FILE_STORAGE_ENDPOINT) used only by the S3Presigner; docker-compose's api service now overrides it to `http://localhost:9000`. Found via manual curl smoke test against the real docker-compose stack, not caught by Testcontainers-based integration tests (which run the presigner against the same host-reachable MinIO Testcontainer for both upload and presign) — reinforces the "integration tests + real docker-compose smoke test" verification pattern from Story 0.3.
- Tests: unit (NominatimGeocodingService via MockRestServiceServer, UploadValidator magic-byte cases, InstallerServiceImpl + CertificationServiceImpl via Mockito) + Testcontainers integration (InstallerCoverageZoneIntegrationTest — coverage-zone set/browse match/radius-exclusion/public-no-auth, using @MockitoBean for GeocodingService to avoid live Nominatim calls in CI; CertificationFlowIntegrationTest — real MinIO Testcontainer, upload/reject-bad-file/admin-approve/non-admin-403). JaCoCo: 91.55% instruction coverage (gate is 80%). Spotless clean.
- Manual smoke test against real `docker compose up` stack (ports 5435/8090, same avoid-port-clash pattern as Story 0.3): real Nominatim geocode of "Rabat, Morocco" succeeded, coverage-zone set, valid-PDF upload (201 PENDING) + disallowed-file-type upload (400) both confirmed, admin MFA-enroll -> list PENDING -> approve (200) -> browse now shows the installer -> audit_log row confirmed via psql, presigned viewUrl fetched successfully (200, correct PDF bytes) after the localhost/minio-hostname fix. Torn down cleanly afterward (`docker compose down`) — only this project's containers touched.

## 2026-07-26 — Session 4: VERIFY phase for Epic 1 (resuming from unconfirmed fork handoff)
- Confirmed scope of uncommitted work matches Epic 1 Stories 1.1-1.3: backend (already logged 2026-07-23) PLUS a full frontend layer that the fork built but never logged — AuthContext/LoginPage/RequireRole (JWT auth, in-memory token per Security doc), installer CoverageZoneForm + CertificationUploadForm + InstallerDashboardPage, admin CertificationQueuePage, public BrowsePage, lib/api.ts (fetch wrapper + ApiError), MUI theme (solar-gold/renewable-green per ui doc).
- Backend: `spotless:check` clean. Unit tests pass (Testcontainers-backed *IntegrationTest/ApiApplicationTests classes skipped in this shell — no Docker daemon here, same known limitation as every prior story; they run in CI). JaCoCo could NOT run locally this session: JaCoCo 0.8.12 can't instrument JDK 25 class files (major version 69) and this machine's JDK is now 25.0.3 (was fine on whatever JDK ran Story 0.1-1.3 previously) while CI pins JDK 21 — pre-existing local/CI tooling mismatch, not an Epic-1 regression. Coverage gate will be confirmed from the CI run instead.
- Frontend: `npm run lint` clean (3 pre-existing informational warnings, no errors). `npm test -- --coverage`: 19/19 tests pass (one `LoginPage` test timed out on the first full-suite run under this sandbox's slow parallel execution; reran in isolation and in the full suite again — passed both times, confirmed flake not a bug). Coverage 90.27% stmts / 80.68% branch / 85.36% funcs / 91.3% lines — meets the 80% gate.
- Security spot-check: diffed all changed/new files for hardcoded secrets/keys/passwords — none found. New backend dependency (aws S3 SDK) already covered by existing Trivy SCA job.
- No new env vars required (FILE_STORAGE_PUBLIC_ENDPOINT already documented in .env.example, defaults to FILE_STORAGE_ENDPOINT).

## 2026-07-26 (cont.) — PUSH
- PUSH: main -> origin/main. Commit 0171b8e (feat(epic-1): installer onboarding & verification). 69 files changed. Monitoring CI next.

## 2026-07-26 (cont.) — CI RED -> fixing security gate
- CI run 30222831263: backend + frontend jobs GREEN (coverage gate passed in CI, confirming local estimates). `security` job RED on the Trivy step: 4 HIGH CVEs (netty, transitive via AWS S3 SDK's netty-nio-client) + 1 HIGH CVE (react-router, GHSA-qwww-vcr4-c8h2). See .logs/decisions.md for the fix/accept reasoning for each. Fixed netty via a netty-bom import (4.2.16.Final); accepted the react-router finding via .trivyignore (RSC-only vuln, this app doesn't use RSC/data routers). Backend rebuild + spotless + unit tests reconfirmed green locally after the netty fix. Pushing follow-up commit and re-monitoring CI per rule 11.
- PUSH: main -> origin/main. Commit 303388c (fix(security): pin netty, accept RSC-only react-router CVE). Monitoring CI.
- CI run 30260129875: all 5 jobs GREEN (backend, frontend, security, build, deploy-staging). CI monitoring complete per rule 11.
- MILESTONE: Epic 1 (Installer Onboarding & Verification — Stories 1.1 coverage-zone/browse, 1.2 cert upload, 1.3 admin review) fully shipped: backend + frontend built, verified (unit tests + coverage gate passed in CI on both stacks), 2 real dependency CVEs triaged (netty fixed, react-router accepted with documented rationale), committed (0171b8e, 303388c), pushed, CI green.

## 2026-07-27 (cont.) — Batch 1: ROI calculator backend (Story 2.1)
- New `com.shamsma.api.roi` package: `RoiCalculator` (pure static calculation — nearest-of-14-Moroccan-cities irradiance lookup via haversine, orientation multiplier, roof-area-capped system sizing, self-consumption-capped savings, nullable payback for non-viable cases), `RoiOrientation` enum, `MoroccoCity` record, `RoiEstimateResponse`, `RoiService`/`RoiServiceImpl` (geocodes via existing GeocodingService, delegates to calculator, translates GeocodingException -> 400 same pattern as InstallerServiceImpl), `RoiController` (`GET /api/v1/roi/estimate`, public, `@Validated` query params).
- Wired into existing infra: added to RateLimitFilter's LIMITED_PATHS and SecurityConfig's permitAll list, same pattern as the browse endpoint.
- Tests: RoiCalculatorTest (7 cases — nearest-city resolution incl. far-south Laayoune, full-usage-coverage invariant across all 4 orientations, orientation cost/payback ordering, null-orientation default, roof-cap binding, zero-bill null-payback edge case), RoiServiceImplTest (2, Mockito), RoiEstimateIntegrationTest (5, Testcontainers + MockMvc — public no-auth, validation 400s, geocoding-failure 400, roof-cap through the full HTTP path). All 9 unit tests pass locally (spotless clean, full unit suite green); integration tests need Docker (unavailable in this shell, same known gap as every prior story — will run in CI).

## 2026-07-27 (cont.) — Batch 2: ROI calculator frontend (Story 2.1) + 2 real bugs found via full Docker verification
- RoiCalculatorPage: address/bill/roof-size/orientation form, result panel (payback/savings/system-size/installation-cost), 20-year net-cash-flow line chart (@mui/x-charts, dataviz skill followed: single-hue green line validated against the app's white surface via validate_palette.js, dashed zero reference line for break-even, chart derived client-side from the API's 3 numeric fields rather than the backend returning a 20-point series). New `/roi` route + nav link (kept `/` as a separate Home page). "See verified installers" deep-links to `/browse?lat=&lng=`.
- Discovered mid-session that this shell's earlier "Docker unavailable" (Bash tool sandbox) was NOT true system-wide — Docker Desktop is running and reachable via PowerShell. Re-ran the full backend suite there for real (Testcontainers + real Postgres/PostGIS, no more skipping integration tests):
  - **Real bug 1**: `RateLimitFilter` keyed its bucket by client IP only, so every rate-limited public endpoint (login, register, browse, and now roi/estimate) shared ONE combined 10-req/min budget per IP — surfaced as 429s once the full test suite's cumulative request count crossed 10/min in the same cached Spring context. Fixed by keying `IP:path` instead — also a real production concern (hammering the public ROI calculator would have throttled logins from the same NAT/IP).
  - **Real bug 2**: `@Validated` constraint violations on individual `@RequestParam`s (RoiController) don't auto-convert to 400 in this Spring Boot 4.1 setup — surfaced as an uncaught `ConstraintViolationException` (500) instead. Added a handler to the existing `GlobalExceptionHandler`.
  - Added RateLimitFilterTest (2 cases, proves per-path isolation) as a regression guard — no dedicated rate-limit test existed before.
  - Full backend suite (59 tests: units + all Testcontainers integration classes across auth/installer/roi) passes clean, real Docker, exit code 0.
- Frontend: full suite 24/24 passing (91.86% stmts overall; RoiCalculatorPage itself 100% line/branch/function per lcov), lint clean.
- Manual live smoke test: spun up the real stack via docker-compose on non-conflicting ports (8099/5490, since 8080/5432/5433/4200 etc. are already used by other unrelated local projects on this machine) — confirmed via curl/PowerShell that the real endpoint (with live Nominatim geocoding) returns correct results (Rabat, 500 MAD/month -> savings=6000 MAD exactly as predicted, payback 7.42yrs). Browser click-through of the live chart itself was inconclusive — repeated CDP screenshot timeouts during form-submit clicks that reproduced even against an unrelated wrong backend, no console errors, no network request ever reaching the API — read as a browser-automation/viewport-scaling tooling issue in this sandbox, not an app bug, given RTL tests exercise the identical fetch-to-chart-render path successfully and repeatedly. Did not chase further per the "stop after repeated tool failures" guidance. Cleaned up: reverted temporary .env CORS addition, `docker compose down` (only this project's containers), stopped the ad-hoc Vite dev server.

## 2026-07-28 — Batch 3: address-based browse (closes Story 2.2's literal gap)
- Backend: `InstallerService`/`InstallerServiceImpl`/`InstallerBrowseController` now accept an `address` query param (geocoded server-side via the existing `GeocodingService`) as an alternative to raw `lat`+`lng`; added validation returning 400 when neither a usable address nor a full lat+lng pair is given. New test cases added to `InstallerServiceImplTest` and `InstallerCoverageZoneIntegrationTest`.
- Frontend: `BrowsePage.tsx` rewritten from raw lat/lng number inputs to a single address field; reads `?lat=&lng=` from the URL so the ROI calculator's "See verified installers" deep link auto-populates and auto-searches. `BrowsePage.test.tsx` rewritten (address search, empty state, URL-param auto-search, geolocation button).
- Full backend suite (real Docker via PowerShell, Testcontainers) confirmed GREEN, exit code 0.

## 2026-07-28 (cont.) — VERIFY phase for Epic 2 (Batches 1-3)
- Frontend: `npm run lint` clean (same 3 pre-existing informational warnings as prior sessions, no errors). `npx vitest run --coverage`: 9/9 test files, 26/26 tests passing. Coverage 94.31% stmts / 85.71% branch / 92.59% funcs / 95.26% lines (see metrics.md for the istanbul-table-vs-lcov reconciliation note).
- Backend: `mvn -DskipTests compile` and `mvn spotless:check` both clean (re-confirms no drift since Batch 3's already-logged full Testcontainers run earlier this session).
- Security spot-check: `git diff` across all changed backend/frontend source plus the new untracked roi/RateLimitFilterTest files, grepped for password/secret/api-key/token/private-key/AWS-key patterns — only false-positive match was a local variable named `accessToken` in a test; no real secrets found.
- All VERIFY gates pass. Proceeding to SHIP (commit + push + CI monitoring per rule 11).
- PUSH: main -> origin/main. Commit 261a329 (feat(epic-2): homeowner discovery — ROI calculator + address-based browse). 31 files changed. Monitoring CI next.
- CI run 30346831850: all 5 jobs GREEN on the first push (backend, security, frontend, build, deploy-staging) — no follow-up fix needed this time. CI monitoring complete per rule 11.
- MILESTONE: Epic 2 (Homeowner Discovery — Story 2.1 ROI/payback calculator, Story 2.2 address-based browse) fully shipped: backend + frontend built, verified, committed (261a329), pushed, CI green.

## 2026-07-28 (cont.) — Epic 3: Quote & Booking Workflow (Stories 3.1-3.3)
- New `com.shamsma.api.booking` package (pre-scaffolded empty since Story 0.1): `QuoteRequest`/`Booking` entities + `QuoteStatus`/`BookingStatus` enums over the existing `quote_requests`/`bookings` tables (V6/V7), `QuoteRequestService`/`Impl` (create per-installer REQUESTED rows with an APPROVED-only check, list-for-homeowner/list-for-installer, respond with ownership+state-transition checks, book with a 10%-of-quote-amount deposit — no deposit-percentage rule exists anywhere in the docs, flagged as a placeholder to revisit at Epic 4/Payments). 5 endpoints: homeowner create/list/book, installer list/respond. One new `SecurityConfig` matcher (`/api/v1/homeowner/**` -> ROLE_HOMEOWNER); installer routes reused the existing `/api/v1/installer/**` matcher.
- Added `getSummary()` cross-package lookups to `InstallerService`/`HomeownerService` (new public `InstallerSummary`/`HomeownerSummary` records) so the booking package can resolve names without reaching into other packages' repositories, per ADR-3.
- `com.shamsma.api.notification` package (also pre-scaffolded, empty): `NotificationService` interface + `LoggingNotificationServiceImpl` — logs instead of sending real email per this session's explicit user decision (deviates from the stories' literal "receives an email notification" AC; SMTP env vars already exist unused, real JavaMailSender impl is a drop-in swap behind the same interface).
- Tests: 11 unit tests (`QuoteRequestServiceImplTest`, Mockito — create/approve-only-check, respond happy paths + ownership 403 + already-responded 409 + missing-amount 400, book happy path + deposit math + non-QUOTED 400 + ownership 403) + 7 Testcontainers integration tests (`QuoteBookingFlowIntegrationTest` — full request->respond->book flow, decline path, IDOR across installers, IDOR across homeowners, invalid-state-transition skip-QUOTED rejection, unapproved-installer rejection) covering every item in the Test Strategy's "Quote/Booking workflow" adversarial checklist. Rewrote the integration test to use `@TestInstance(PER_CLASS)` with actors registered once in `@BeforeAll` after the first draft's per-test-method registration volume (~15-20 calls across 7 tests) tripped RateLimitFilter's 10/min `/api/v1/auth/register` budget — a real rate limiter correctly doing its job against test volume, not an app bug. Full backend suite (82 tests, real Docker via PowerShell) green.
- Frontend: `BrowsePage.tsx` gained a role-gated "Request quote" button per installer card (homeowners only); new `features/homeowner/HomeownerRequestsPage.tsx` (own requests list + Book action when QUOTED) at a new `/homeowner/requests` route; new `features/installer/QuoteRequestInbox.tsx` composed into the existing `InstallerDashboardPage` (lead list + inline quote-amount field + Send quote/Decline actions). Frontend suite: 11 files / 34 tests passing, 94.93% stmts / 84.21% branch coverage (both gates met).
- Security spot-check: diffed all changed/new files for hardcoded secrets — only match was the same test-fixture password literal already used across every prior integration test class.
- PUSH: main -> origin/main. Commit fb30900 (feat(epic-3): quote & booking workflow). 37 files changed. Monitoring CI next.
- CI run 30359675614: all 5 jobs GREEN on the first push (backend, frontend, security, build, deploy-staging) — backend's JaCoCo coverage gate confirms the 80% threshold in CI, closing out the local-JaCoCo-couldn't-run gap. CI monitoring complete per rule 11.
- MILESTONE: Epic 3 (Quote & Booking Workflow — Stories 3.1 request quotes, 3.2 installer respond, 3.3 homeowner books) fully shipped: backend + frontend built, verified, committed (fb30900), pushed, CI green.

## 2026-07-28 (cont.) — Epic 4: Payments (Stories 4.1-4.2; 4.3 deferred)
- New `payment` package (pre-scaffolded, empty): `Payment` entity/enum over the existing `payments` table (V7); `CmiCheckoutService` interface + `MockCmiCheckoutServiceImpl` (no real CMI merchant account exists — see this session's Epic 4 BRAINSTORM/PLAN log entries); `CmiSignatureService` (HMAC-SHA256, constant-time `MessageDigest.isEqual` comparison); `PaymentService`/`Impl` (checkout creation with DB-unique-constraint-backed double-click safety — catches `DataIntegrityViolationException` on the race rather than relying on an app-level check-then-act; webhook processing with signature verification, idempotency on already-SUCCEEDED transaction IDs, amount-mismatch rejection). New endpoints: `POST /api/v1/homeowner/bookings/{id}/checkout`, `POST /api/v1/payments/webhook` (public, signature-verified), `POST /api/v1/mock-cmi/{transactionId}/succeed|fail` (public, dev-only — builds a real signed payload and calls the exact same `processWebhook()` the real webhook would hit).
- New `BookingService`/`Impl` in the `booking` package (kept separate from the already-shipped `QuoteRequestService`) exposing `getOwnedBooking`/`markPaid` for the payment package per ADR-3, plus a `GET /api/v1/homeowner/bookings/{id}` endpoint — a real gap found while writing tests: there was no way for a homeowner (or the frontend) to check a single booking's status after checkout.
- Real judgment call made without a full re-ask: amount-mismatch webhooks are logged (SLF4J WARN) rather than written to `audit_log`, since `audit_log.actor_user_id` is `NOT NULL` FK'd to `users` and a webhook has no real "actor" user to attribute it to — a dedicated review surface is Epic 5 (Admin Oversight) territory anyway.
- Structural test-infrastructure fix: found that `RateLimitFilter`'s in-memory bucket is a singleton shared by every test class in the same cached Spring test context (not just within one class) — enough integration tests hitting `/api/v1/auth/register`/`/api/v1/payments/webhook` across the WHOLE suite legitimately exhausted the real 10/min production budget, first surfacing as a `QuoteBookingFlowIntegrationTest` failure caused by test order, not by anything wrong in that test itself. Fixed properly (not by weakening the production limiter): made capacity/refill-period `@Value`-configurable (defaults unchanged, 10/min), and added `@SpringBootTest(properties = "app.rate-limit.capacity=1000")` uniformly across all 7 integration test classes so they still share one cached context with real Docker/Postgres.
- Tests: 22 new unit tests (`CmiSignatureServiceTest`, `PaymentServiceImplTest`, `BookingServiceImplTest`) + 9 Testcontainers integration tests (`PaymentFlowIntegrationTest` — full checkout->mock-succeed->BOOKED flow, fail-then-retry-succeeds, IDOR on checkout/view, re-checkout-after-paid rejected, unsigned/invalid-signature/unknown-transaction webhook rejections, amount-mismatch rejection, duplicate-webhook idempotency) covering every item in the Test Strategy's payment adversarial checklist and all 3 ATDD payment scenarios. Full backend suite (113 tests, real Docker via PowerShell) green.
- Frontend: new `PaymentCheckoutPage` (`/homeowner/bookings/:bookingId/checkout`) — shows deposit amount, "Simulate successful/failed payment" buttons calling the mock-cmi endpoints directly, refetches booking status after each simulation. `HomeownerRequestsPage`'s "Book" button now navigates here on success. Frontend suite: 12 files / 38 tests passing, 94.97% stmts / 83.52% branch coverage (both gates met).
- Story 4.3 (nightly reconciliation) explicitly deferred per BRAINSTORM/PLAN — user agreed it can't be meaningfully built against a self-consistent mock CMI transaction log.
- Security spot-check: diffed all changed/new files for hardcoded secrets — only matches were empty `CMI_*` env placeholders, comments, and test-fixture literals (password/secret constants already used the same way across every prior test class).
- PUSH: main -> origin/main. Commit 0aecae0 (feat(epic-4): payments (CMI checkout + webhook, mocked gateway)). 45 files changed.
- CI run 30368733132: all 5 jobs GREEN on the first push (backend, frontend, security, build, deploy-staging) — backend's JaCoCo coverage gate confirms 80%+ in CI. CI monitoring complete per rule 11.
- MILESTONE: Epic 4 (Payments — Stories 4.1 CMI checkout, 4.2 webhook handling; 4.3 deferred) fully shipped: backend + frontend built, verified, committed (0aecae0), pushed, CI green.

## 2026-07-28 (cont.) — Epic 5 PLAN confirmed, starting EXECUTE
Batches 1-4 approved. DEFERRED: rule-9 Playwright video recording (project-version completion, since Epic 5 is the last story in docs/stories-shams-ma.md) — no Playwright setup exists in repo; user chose to skip for this session and track as a follow-up rather than add new tooling now.

## 2026-07-28 (cont.) — Epic 5 Batch 1+2 backend built, session ending mid-verify
Batch 1 (PaymentReviewFlag data layer) and Batch 2 (AdminBookingOverviewService/Controller, PaymentReviewFlagService/Controller) fully implemented and backend-compiles clean. New files: payment/{PaymentReviewFlag,PaymentReviewFlagStatus,PaymentReviewFlagRepository,PaymentReviewFlagService,PaymentReviewFlagServiceImpl,PaymentSummary}.java, booking/{AdminBookingOverviewRow,AdminBookingOverviewFilter,AdminBookingOverviewService,AdminBookingOverviewServiceImpl}.java, admin/{AdminBookingOverviewController,PaymentReviewFlagController}.java, migration V11__create_payment_review_flags.sql. PaymentServiceImpl now persists an OPEN flag (not just log.warn) on webhook amount mismatch, and exposes findByBookingIds() for the admin join.
Tests added: PaymentServiceImplTest (+2 tests), PaymentReviewFlagServiceImplTest (4 tests, new file), AdminBookingOverviewServiceImplTest (4 tests, new file), AdminBookingOverviewIntegrationTest (4 tests, new file in admin package — full booking+payment+admin-login flow via Docker/Testcontainers), extended PaymentFlowIntegrationTest's mismatch test to assert a flag row is created.
Full backend suite run just before session end: ALL GREEN except ONE known, understood failure — PaymentFlowIntegrationTest.webhookWithAMismatchedAmountIsRejected now asserts `expected_amount = 30000` but the actual stored value is 3000.00 (deposit = 10% of the 30000 quote amount per QuoteRequestServiceImpl's DEPOSIT_RATE, not the raw quote amount) — this is a test-assertion bug I introduced this session, NOT a production bug. AdminBookingOverviewIntegrationTest (the new admin-facing test) is fully green (4/4), confirming the actual feature works.
Also noted mid-session: Docker Desktop was NOT running at the start of the VERIFY pass this time (unlike prior sessions where it was already up) — had to Start-Process it and poll ~5s before Testcontainers could connect. Not a code issue, just an environment state to check first next time.
Next session — FIRST STEPS:
1. Fix PaymentFlowIntegrationTest.java:256's assertion: change `expected_amount = 30000` to `expected_amount = 3000` (or `3000.00`) to match the real 10% deposit amount.
2. Re-run full backend suite (`./mvnw.cmd -q test` via PowerShell from the `backend` dir — check Docker Desktop is up first with `docker ps`) to confirm all-green.
3. Continue Batch 3 (frontend: AdminBookingsOverviewPage.tsx, admin nav route, component tests) — NOT STARTED yet.
4. Then Batch 4: VERIFY (frontend lint/vitest/coverage, backend spotless+coverage-via-CI since local JaCoCo still can't run on JDK25), commit, push, monitor CI to green.
No commits made this session — everything above is uncommitted in the working tree (new files under backend/src/main and backend/src/test, plus the two edited existing files PaymentServiceImpl.java/PaymentService.java/PaymentRepository.java and PaymentFlowIntegrationTest.java/PaymentServiceImplTest.java).

## 2026-07-29 — Session resumed, Epic 5 finished and shipped
Fixed the queued test-assertion bug (PaymentFlowIntegrationTest.java:254, `expected_amount = 30000` -> `3000`) and re-ran the full backend suite (Docker Desktop needed a manual start first, same as noted last session) — surfaced a REAL production bug, not just a test bug: `PaymentServiceImpl.processWebhook()` is `@Transactional` and threw `ResponseStatusException` right after `paymentReviewFlagRepository.save()` on an amount mismatch; Spring's default rollback-on-unchecked-exception was silently discarding that save along with the 400 response, so the review-flag row this whole epic exists to create was NEVER actually persisted in production, only in the one test that (with the wrong assertion) happened to mask it. Fixed with `@Transactional(noRollbackFor = ResponseStatusException.class)` — safe here since every other early-exit `ResponseStatusException` in that method (bad signature, malformed payload, unknown transaction) throws before any write. Full backend suite (126 tests) green after the fix.
Batch 3 (frontend): `AdminBookingsOverviewPage.tsx` — paginated/filterable booking+payment table (bookingStatus/paymentStatus/needsReviewOnly/search filters, `TablePagination`), inline Resolve/Dismiss actions on rows with an open review flag, CSV export (raw `fetch` + Blob download, since the existing `apiFetch` wrapper always calls `.json()` and can't handle a CSV response). Wired into `App.tsx` (`/admin/bookings` route + nav link, `RequireRole role="ADMIN" requireMfa"`, matching the certifications page's pattern). 7 new tests (`AdminBookingsOverviewPage.test.tsx`): list, empty state, filter refetch, resolve, dismiss, CSV export, page-size change.
Real bug #2 found while writing the filter tests: every filter/search keystroke changed the React Query key, and without `placeholderData`, TanStack Query v5 treats a new key as a fresh un-cached query — `isLoading` went true and the ENTIRE page (including the filter controls) was replaced by a bare spinner on every keystroke, an unusable UX regression that only showed up because the test tried to interact with a checkbox that had just been unmounted. Fixed with `placeholderData: keepPreviousData` so the previous page's rows stay visible (only a background refetch) while filters change.
VERIFY: backend `spotless:apply` needed (the uncommitted Batch 1/2 backend code from last session had never been run through the formatter) — reformatted, `spotless:check` clean, full backend suite re-confirmed green after formatting (126 tests). Frontend: lint clean (same 4 pre-existing informational warnings, no errors), full suite 45/45 tests green, coverage 93.05% stmts / 82.05% branch / 92.63% funcs / 95.52% lines (both gates well above 80%; the new page's own coverage is 83.67% stmts after the added filter/dismiss/export/pagination tests). Security spot-check: diffed all changed backend/frontend source for secret patterns — only match was a pre-existing test-fixture `SECRET = "test-secret"` constant in `PaymentServiceImplTest`, same pattern used across every prior payment test class. No new env vars required.
- PUSH: main -> origin/main. Commit 539e3f0 (feat(epic-5): admin oversight — booking/payment overview + review-flag resolution). 27 files changed. Monitoring CI next.
- CI run 30446201159: backend/frontend/security GREEN, but `build` job RED — `docker build` on the frontend image ran `npm run build` (`tsc -b && vite build`), which neither local lint nor vitest exercise (vitest transpiles with esbuild, not tsc). Real gap: `AdminBookingsOverviewPage.tsx` passed `alignItems`/`flexWrap` directly as `Stack` props; this codebase's installed MUI version only accepts those via `sx` (every other `Stack` usage in the repo already does this) — direct props broke tsc's overload resolution with a misleading "Property 'alignItems' does not exist" error. Reproduced locally with `npm run build` (never run locally before pushing — added to this session's VERIFY, and saved as a standing memory for future sessions). Fixed by moving both props into `sx={{ alignItems: 'center', flexWrap: 'wrap' }}`.
- PUSH: main -> origin/main. Commit 7a0c3e0 (fix(admin): pass Stack layout props via sx to satisfy tsc build). Monitoring CI.
- CI run 30447253543: all 5 jobs GREEN (backend, frontend, security, build, deploy-staging). CI monitoring complete per rule 11.
- MILESTONE: Epic 5 (Admin Oversight — Story 5.1 booking/payment overview) fully shipped: backend + frontend built, verified, 1 real production bug fixed (webhook amount-mismatch review flag was being rolled back and never persisted), 1 real CI-only build bug fixed (MUI Stack prop typing), committed (539e3f0, 7a0c3e0), pushed, CI green.
- MILESTONE: docs/stories-shams-ma.md is now fully built — Epics 0-5 (all stories) shipped. This closes the MVP scope as originally planned in Sprint 1.
- DEFERRED (carried from Epic 5 PLAN): rule-9 Playwright video recording for this MVP version-completion milestone — no Playwright setup exists in the repo; skipped this session per explicit user choice, tracked as an open follow-up rather than done now.
- Still-open longstanding items, none blocking: skills/ -> .claude/.skills/ duplication (repeatedly deferred), local JaCoCo/JDK25 mismatch (verify via CI), possible react-router-dom 7->8 bump to drop the .trivyignore entry, no real CMI merchant account (Epic 4's mock boundary), 10%-deposit-rate placeholder with no documented business rule behind it.

## 2026-07-29 (cont.) — Post-MVP cleanup: closed all deferred items except the CMI merchant account
User asked to resolve every longstanding open item except "no real CMI merchant account" (that one needs an actual bank relationship, not something buildable). Went through each:
- DECISION: deposit rate — user confirmed 10% via AskUserQuestion (keep as-is, document as the real business rule rather than a placeholder). Updated `QuoteRequestServiceImpl`'s comment and added the rate to `docs/prd-shams-ma.md` FR-8.
- `skills/` -> `.claude/.skills/` duplication: investigated and found already resolved — `skills/` no longer exists on disk, `.claude/.skills/` is the sole copy and is fully git-tracked (not untracked as the original note assumed). No action needed; this was stale information carried in the logs since Sprint 2.
- JaCoCo/JDK25: bumped `jacoco-maven-plugin` 0.8.12 -> 0.8.14 (backend/pom.xml) — 0.8.14 added Java 25 class-file support (confirmed via web search). Verified locally: `mvn test jacoco:report jacoco:check` now runs clean on this machine's JDK 25.0.3 with no instrumentation errors, 94% instruction coverage. Closes a recurring "verify via CI only" workaround used since Epic 1.
- react-router-dom v7 -> react-router v8 (drops the GHSA-qwww-vcr4-c8h2 .trivyignore entry): v8 removes the separate `react-router-dom` package (merged into `react-router`); this app uses declarative `<BrowserRouter>`/`<Routes>`, not a data router, so the migration was a straight import-path swap (`react-router-dom` -> `react-router`) across 13 files + `package.json`. v8 requires Node >=22.22 (this app's CI/Dockerfile were still on Node 20) — bumped `.github/workflows/ci.yml`, `frontend/Dockerfile`, and `docs/devops-shams-ma.md` to Node 22. Verified: local `npm run build`/lint/vitest (45/45) all green, plus a real local `docker build` of the frontend image to confirm the Node 22 base image + v8 actually work together (this is exactly the class of failure that broke CI on the Epic 5 push two sessions ago). Removed the now-moot CVE entry from `.trivyignore`.
- Playwright golden-path recording (rule 9, deferred from Epic 5): added `@playwright/test`, `frontend/playwright.config.ts` (video: 'on'), `frontend/e2e/golden-path.spec.ts`. Discovered a real, previously unnoticed product gap while writing it: this app has NO frontend registration or MFA-enrollment UI at all — `RegisterRequest.Role` only allows HOMEOWNER/INSTALLER (admin accounts are explicitly "provisioned out-of-band" per an existing code comment) and `LoginPage.tsx` is the only auth screen that exists. Account creation in the spec goes through the API directly (matching how the real app actually works today); the recorded browser flow covers everything that *does* have a UI: installer coverage-zone+cert upload, admin cert approval, homeowner browse+quote request, installer quote response, homeowner book+pay (mock CMI), admin bookings overview. Excluded the ROI calculator (standalone lead-gen tool, not part of the core booking path) to keep scope focused.
  - Stood up a real (non-Docker, host-based) environment for the recording: Postgres+PostGIS and MinIO via `docker compose up -d db minio` (DB_HOST_PORT=5491 override — default 5432/8080/4200 are all taken by other local projects on this machine), backend via `mvnw spring-boot:run` (SERVER_PORT=8098, DB_PORT=5491, CORS_ALLOWED_ORIGINS=http://localhost:5173), frontend via `npm run dev` (VITE_API_BASE_URL=http://localhost:8098 via a temporary gitignored `.env.local`). Seeded one pre-MFA-enrolled admin row directly via `docker exec psql` (bcrypt hash computed with the app's own `BCryptPasswordEncoder` via `jshell` against the Maven classpath, not a different bcrypt implementation) — confirmed via code reading that MFA enrollment is a one-time API-only setup step, not a per-login TOTP challenge (`isMfaEnrolled()` is just `mfaSecret != null`), so a pre-enrolled admin logging in through the real UI is fully representative of actual usage.
  - Two real spec bugs found and fixed while iterating against the live app (not app bugs): (1) `LoginPage`'s post-login `navigate('/')` races with an immediately-following nav-link click in the test — fixed by awaiting `page.waitForURL('/')` after every login; (2) nav items render as `<Link>` (role="link"), not buttons — several locators were wrongly scoped to `getByRole('button', ...)`. Also had to make homeowner/installer names unique per run (`Date.now()`-suffixed) and scope several assertions to the specific row/card, because the shared Postgres dev volume has leftover data from past manual smoke-test sessions (`Solaire Atlas`/`Yasmine Alaoui` already existed from prior sessions) — a real, if minor, hygiene gap in how this project's manual smoke tests clean up after themselves.
  - Test passed end-to-end on the second real attempt; video copied to `.recordings/v1.0-2026-07-29.webm` (gitignored per the repo's existing "large binaries, not git" policy — nothing to commit for the video itself). Environment torn down cleanly afterward (killed the backend/frontend process trees, `docker compose down`, removed the temporary `.env.local`, jshell/classpath scratch files, and Playwright's `test-results`/`playwright-report` output dirs).
  - Also excluded `frontend/e2e/**` from Vitest's own test discovery (`vite.config.ts`) — without it, `vitest run` tried to execute the Playwright spec directly and failed with "Playwright Test did not expect test() to be called here."
VERIFY (this whole batch): backend `spotless:apply` (one doc-comment formatting fix) + `spotless:check` clean + full suite (126 tests, real Docker) green. Frontend: lint clean (same 4 pre-existing warnings), `npm run build` (tsc+vite) clean, full vitest suite 45/45 green, coverage unchanged at 93.05%/82.05%/92.63%/95.52%. Security spot-check: diffed the full batch's changed/new files for secret patterns — only match was a test-fixture default password literal in the new E2E spec (`E2eDemoPass!2026`), same non-sensitive-fixture pattern used everywhere else in this project's tests. Confirmed `@playwright/test` has no postinstall browser-download hook, so it's safe to add as a devDependency without affecting `npm ci` in CI/Docker.
